dist/
.DS_Store
__pycache__/
venv/

# Secrets — never commit these
.env
*.env
!.env.example
!*.env.example
serviceAccount*.json
firebase-credentials*.json
*-service-account*.json

# IB Gateway (Docker) persisted settings / session
tws_settings/
