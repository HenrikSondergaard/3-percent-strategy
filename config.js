// Cloudflare Worker URL for triggering data refresh
// Set this after deploying the worker in step 6
// Example: WORKER_URL = 'https://spx-refresh.your-subdomain.workers.dev'
WORKER_URL = 'https://spx-refresh.henriksondergaard.workers.dev';
